<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mab.belal.madarweatherdemo">

    <application
        android:name=".di.DaggerApplication"
        android:allowBackup="true"
        android:icon="@drawable/weather_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/weather_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


    <activity
        android:name=".view.splash.SplashActivity"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity android:name=".view.list_cities.CityListActivity" />

    <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="@string/google_maps_key" />

    <activity
        android:name=".view.add_city.AddCityActivity"
        android:label="@string/title_activity_add_city"></activity>

    </application>

</manifest>